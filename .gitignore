*.pem
*.token
*.key
.env
.env.*
!.env.example
node_modules/
.DS_Store
gates.log
derive.log
# The bundle a local run writes at the repository root. Anchored with a leading
# slash on purpose: an unanchored `evidence/` would also hide engine/evidence/,
# which is the writer's source and must be committed.
/evidence/
__pycache__/
*.pyc

# security/eyes-only -- preventive hardening staged 2026-09-04 (estate security sweep, Master order
# 2026-09-04). No file matching these was found tracked at sweep time; this refuses one by path
# before it can ever be staged again. See work/reports/2026-09-04-security-eyes-only-on-github.md.
/work/
/operations/
